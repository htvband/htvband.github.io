block vars
  - var active = "bio";

extends base

prepend head

block content
  div#main.body.home.bg-white
    section#content.bg-white
      != markdown.toHTML(fs.readFileSync(content + 'bio.md', 'utf8'))